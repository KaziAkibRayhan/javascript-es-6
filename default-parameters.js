function addition(first = 0, second = 0) {
    return first + second;
}
// console.log(addition(23));

function makeFullName(first, last = 'Kazi') {
    return first + ' ' + last;
}
console.log(makeFullName('Akib'));